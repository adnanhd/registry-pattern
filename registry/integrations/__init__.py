"""Framework integrations for registry-pattern.

Thin wiring that exposes registry-pattern's machinery to other libraries. Import
the specific integration you need, e.g.::

    from registry.integrations.pydantic import ArtifactOf
"""
